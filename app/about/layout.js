export const metadata = {
  title: "About me",
  description: "The person and story behind the Lepigonia travel journal.",
};

export default function AboutLayout({ children }) {
  return children;
}
