export const metadata = {
  title: "Login",
  description: "Sign in to the Lepigonia personal area.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
