"use client";
import {useEffect,useState} from "react";import Link from "next/link";import "./admin.css";
const blank=()=>({slug:"",title:"",date:new Date().toISOString().slice(0,10),location:"",country:"",lat:"",lng:"",image:"",excerpt:"",content:""});const MAX=850*1024;
function prepare(file){return new Promise((resolve,reject)=>{if(file.type==="image/gif"){if(file.size>MAX)return reject(new Error("GIF-Datei bitte kleiner als 850 KB wählen."));const r=new FileReader();r.onload=()=>resolve({data:r.result,filename:file.name});r.onerror=()=>reject(new Error("Bild konnte nicht gelesen werden."));r.readAsDataURL(file);return}const img=new Image(),r=new FileReader();r.onload=()=>{img.onload=()=>{const scale=Math.min(1,1800/Math.max(img.naturalWidth,img.naturalHeight)),c=document.createElement("canvas");c.width=Math.max(1,Math.round(img.naturalWidth*scale));c.height=Math.max(1,Math.round(img.naturalHeight*scale));c.getContext("2d").drawImage(img,0,0,c.width,c.height);let q=.76;const go=()=>c.toBlob(b=>{if(!b)return reject(new Error("Bild konnte nicht verarbeitet werden."));if(b.size<=MAX||q<=.34){if(b.size>MAX)return reject(new Error("Das Bild konnte nicht klein genug komprimiert werden."));const rr=new FileReader();rr.onload=()=>resolve({data:rr.result,filename:file.name.replace(/\.[^.]+$/i,"")+'.webp'});rr.readAsDataURL(b)}else{q-=.06;go()}},"image/webp",q);go()};img.onerror=()=>reject(new Error("Bild konnte nicht gelesen werden."));img.src=r.result};r.onerror=()=>reject(new Error("Bild konnte nicht gelesen werden."));r.readAsDataURL(file)})}
export default function Admin(){const[ok,setOk]=useState(null),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[posts,setPosts]=useState([]),[post,setPost]=useState(blank()),[countries,setCountries]=useState([]),[gallery,setGallery]=useState(null),[error,setError]=useState(""),[status,setStatus]=useState("");
async function load(){const r=await fetch("/api/admin/posts",{cache:"no-store"});if(!r.ok){setOk(false);return}setPosts((await r.json()).posts||[]);setOk(true);const g=await fetch("/api/admin/gallery",{cache:"no-store"});if(g.ok){const d=await g.json();setCountries(d.countries||[]);if(!gallery&&d.countries?.[0])setGallery(d.countries[0].slug)}}useEffect(()=>{load()},[]);
async function login(e){e.preventDefault();const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(!r.ok){setError((await r.json()).error);return}await load()}
async function save(e){e.preventDefault();setError("");setStatus("Speichere …");const r=await fetch("/api/admin/posts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(post)}),d=await r.json();if(!r.ok){setError(d.error);setStatus("");return}setStatus("Gespeichert – Vercel veröffentlicht automatisch.");await load()}
async function del(){if(!confirm("Beitrag wirklich löschen?"))return;const r=await fetch("/api/admin/posts",{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({slug:post.slug})});if(!r.ok){setError((await r.json()).error);return}setPost(blank());await load()}
async function articleUpload(e){const f=e.target.files?.[0];if(!f)return;await uploadPrepared(f,"/api/admin/upload",d=>setPost(p=>({...p,image:d.url})),"Bild");e.target.value=""}
async function galleryUpload(e){const files=[...(e.target.files||[])];if(!files.length||!gallery)return;setError("");try{for(let i=0;i<files.length;i++){setStatus(`${i+1} / ${files.length} Galeriebilder werden verarbeitet …`);const p=await prepare(files[i]);const r=await fetch("/api/admin/gallery",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"image-add",slug:gallery,data:p.data})});const d=await r.json();if(!r.ok)throw new Error(d.error||`Bild ${i+1} fehlgeschlagen`)}await load();setStatus(`${files.length} Galeriebild(er) hinzugefügt.`)}catch(err){setError(err.message);setStatus("")}e.target.value=""}
async function uploadPrepared(f,url,onSuccess,label){setError("");setStatus(`${label} wird vorbereitet …`);try{const p=await prepare(f);setStatus(`${label} wird hochgeladen …`);const r=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(p)});const d=await r.json();if(!r.ok)throw new Error(d.error||`Upload fehlgeschlagen (${r.status})`);onSuccess(d);setStatus(`${label} hochgeladen – jetzt speichern.`)}catch(err){setStatus("");setError(err.message)}}
async function galleryAction(body){setError("");const r=await fetch("/api/admin/gallery",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),d=await r.json();if(!r.ok){setError(d.error);return}if(body.action==="country-update"&&d.slug)setGallery(d.slug);await load()}
if(ok===null)return <div className="admin-loading">Lepigonia Admin …</div>;if(!ok)return <main className="admin-login"><Link href="/" className="admin-brand">Lepigonia</Link><div className="login-card"><p className="admin-eyebrow">Private area</p><h1>Admin Login</h1><p>Geschichten und Galerie verwalten.</p><form onSubmit={login}><label>E-Mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Passwort<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button>Einloggen →</button>{error&&<p className="admin-error">{error}</p>}</form></div></main>;
const current=countries.find(c=>c.slug===gallery);const editing=post.slug||post.title;
return <main className="admin-shell"><header className="admin-top"><div><Link href="/" className="admin-brand">Lepigonia</Link><span className="admin-top-label"> / Admin</span></div><nav className="admin-top-nav"><Link href="/gallery" target="_blank">Galerie ↗</Link><Link href="/map" target="_blank">Karte ↗</Link><Link href="/" target="_blank">Website ↗</Link><button onClick={async()=>{await fetch("/api/admin/logout",{method:"POST"});setOk(false)}}>Logout</button></nav></header><div className="admin-grid"><aside className="admin-sidebar"><b>Stories</b><button className="new-button" onClick={()=>{setPost(blank());setError("")}}>＋ Neue Geschichte</button>{posts.map(p=><button key={p.slug} className={`post-nav ${post.slug===p.slug?"active":""}`} onClick={()=>setPost(p)}><strong>{p.title||p.slug}</strong><small>{p.country||p.location||"Ohne Land"}</small></button>)}<hr/><b>Galerie</b><button className="new-button" onClick={()=>{const n=prompt("Neues Land:");if(n)galleryAction({action:"country-create",name:n})}}>＋ Neues Land</button>{countries.map(c=><button key={c.slug} className={`post-nav ${gallery===c.slug&&!editing?"active":""}`} onClick={()=>{setGallery(c.slug);setPost(blank())}}><strong>{c.name}</strong><small>{c.images?.length||0} Bilder</small></button>)}</aside>{editing?<section className="editor"><div className="editor-head"><div><p className="admin-eyebrow">Editor</p><h1>{post.slug?"Geschichte bearbeiten":"Neue Geschichte"}</h1></div>{post.slug&&<button className="danger" onClick={del}>Löschen</button>}</div><form onSubmit={save} className="editor-form"><div className="field-row"><label>Titel<input value={post.title} onChange={e=>setPost({...post,title:e.target.value})} required/></label><label>Slug<input value={post.slug} onChange={e=>setPost({...post,slug:e.target.value.toLowerCase().replace(/[^a-z0-9-]+/g,"-")})} required/></label></div><div className="field-row"><label>Datum<input type="date" value={post.date} onChange={e=>setPost({...post,date:e.target.value})}/></label><label>Ort<input value={post.location} onChange={e=>setPost({...post,location:e.target.value})}/></label></div><label>Land<select value={post.country} onChange={e=>setPost({...post,country:e.target.value})}><option value="">Land wählen</option>{countries.map(c=><option key={c.slug} value={c.name}>{c.name}</option>)}</select></label><div className="field-row"><label>Breitengrad (Lat)<input type="number" step="any" min="-90" max="90" placeholder="z.B. 38.7223" value={post.lat||""} onChange={e=>setPost({...post,lat:e.target.value})}/></label><label>Längengrad (Long)<input type="number" step="any" min="-180" max="180" placeholder="z.B. -9.1393" value={post.lng||""} onChange={e=>setPost({...post,lng:e.target.value})}/></label></div><p className="upload-hint">Diese Koordinaten bestimmen später exakt die Position des Pins auf der Weltkarte.</p><label>Teaser<input value={post.excerpt} onChange={e=>setPost({...post,excerpt:e.target.value})}/></label><label>Bild-URL<input value={post.image} onChange={e=>setPost({...post,image:e.target.value})}/></label><label className="upload">Bild hochladen<input type="file" accept="image/*" onChange={articleUpload}/></label><p className="upload-hint">Große iPhone/iPhoto-Fotos werden automatisch auf WebP und webtaugliche Größe optimiert.</p><label>Text (Markdown)<textarea className="content-editor" value={post.content} onChange={e=>setPost({...post,content:e.target.value})}/></label><div className="save-row"><button className="save">Speichern & veröffentlichen →</button>{status&&<span className="status">{status}</span>}</div>{error&&<p className="admin-error">{error}</p>}</form></section>:<section className="editor"><div className="editor-head"><div><p className="admin-eyebrow">Galerie</p><h1>{current?.name||"Galerie"}</h1><p>Blogbilder erscheinen automatisch. Zusätzliche Fotos kannst du hier unabhängig hochladen.</p></div></div>{current&&<><label className="upload gallery-upload">＋ Mehrere Bilder hinzufügen<input type="file" accept="image/*" multiple onChange={galleryUpload}/></label><p className="upload-hint">Mehrere große iPhone/iPhoto-Originale können gleichzeitig ausgewählt werden.</p><div className="story-grid">{current.images?.map(i=><div className="story-card" key={i.id}><div className="story-media"><img src={i.url} alt={i.title||"Galeriebild"}/></div><div className="story-meta"><span>{i.source==="manual"?"Manuell":"Blogartikel"}</span>{i.source==="manual"&&<button className="danger" onClick={()=>{if(confirm("Bild wirklich löschen?"))galleryAction({action:"image-delete",slug:current.slug,id:i.id})}}>Löschen</button>}</div></div>)}</div><hr/><div className="field-row gallery-settings"><label>Land umbenennen<input defaultValue={current.name} onBlur={e=>{if(e.target.value!==current.name)galleryAction({action:"country-update",slug:current.slug,name:e.target.value})}}/></label><button className="danger" onClick={()=>{if(confirm(`Land ${current.name} löschen? Blogbilder bleiben erhalten.`))galleryAction({action:"country-delete",slug:current.slug})}}>Land löschen</button></div></>}</section>}</div></main>}
